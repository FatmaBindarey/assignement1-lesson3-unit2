*/

#include <stdio.h>
#include <stdlib.h>

int main(void)
{
	int a=10;
	int b=5;
	printf(" two swap numbers:");
	fflush(stdin); 
  fflush(stdout);
	a=a+b;
	a=a-b;
	a=a-b;
	printf(" the value after swapping %d \r\n %d",y,x);
return 0;
}
